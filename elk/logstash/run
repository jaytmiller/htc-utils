#! /bin/sh  -eu

source $1;
shift;

sudo docker run -d \
       --name ${HTC_LOGSTASH_CONTAINER_NAME} \
       -p 5044:5044 \
       -p 9600:9600 \
       --mount type=bind,source=${HTC_CONDOR_LOGS},destination=/var/log/condor,readonly \
       ${HTC_IMAGE_NAME}  $*

